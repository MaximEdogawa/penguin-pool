export interface Windows95Theme {
  id: string
  name: string
  description: string
  category: 'retro'
  colors: {
    primary: string
    secondary: string
    background: string
    surface: string
    text: string
    border: string
    accent: string
  }
}

'use client'

// Re-export the generic AssetSelector for backward compatibility
export {
  default,
  type ExtendedAsset as ExtendedOfferAsset,
  type AssetSelectorProps,
} from '@/components/shared/AssetSelector'
